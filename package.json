{
  "name": "turkey",
  "version": "1.0.0",
  "description": "An Amazon Mechanical Turk turn-key segment tool",
  "scripts": {
    "start": "http-server src -o -c-1",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "http-server": "^14.1.1",
    "vitest": "^1.3.0"
  }
}
